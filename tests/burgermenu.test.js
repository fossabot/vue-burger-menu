import { mount, shallow } from "vue-test-utils";
import { createRenderer } from "vue-server-renderer";

import BurgerMenu from "../src/BurgerMenu.vue";

describe("BurgerMenu", () => {
  it("should add two numbers", () => {
    expect(1 + 2).toBe(3);
  });
});
